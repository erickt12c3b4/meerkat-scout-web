# Launch checklist (from the handoff README, with status)

Status as of 2026-09-06. "Where" tells the next person exactly what to change.

| # | Item | Status | Where / what |
| --- | --- | --- | --- |
| 1 | Production reCAPTCHA site key (Contact + Book a Demo) and server-side verification | **Open — code ready, key missing.** Both forms run on Google's public TEST key (always passes, shows the "for testing purposes only" banner). Server-side verification is implemented in `app/api/lead/route.ts` (siteverify) and runs for every demo submission; Contact still delivers by `mailto:` and does not hit the API. | Register the domain in the reCAPTCHA admin console and set `NEXT_PUBLIC_RECAPTCHA_SITE_KEY` + `RECAPTCHA_SECRET`. Google issues "v2 checkbox" and "v2 invisible" as separate key types; `/contact` uses the checkbox widget and `/demo` the invisible one, so either create one key of each type and split the constant in `lib/recaptcha.ts`, or move both forms to the same widget type. Decide on `ACCEPT_UNTOKENED` in the route (currently accepts untokened leads flagged `verified:false`). |
| 2 | `leadEndpoint` for the demo form (CRM / webhook) | **Open — stub.** `/api/lead` verifies the token, then forwards JSON to `LEAD_ENDPOINT` when set. When unset it logs and returns `delivered:false`, and the browser falls back to a prefilled `mailto:support@meerkatscout.com` draft exactly as the design specifies. | Set `LEAD_ENDPOINT` (server env). Payload: `{ kind, name, email, shop, monthlyOrders, channels[], notes, page, submittedAt, utm_*, verified, receivedAt, ip }`. |
| 3 | `schedulerUrl` (Cal.com connected to Google Calendar) | **Open.** With `NEXT_PUBLIC_SCHEDULER_URL` unset the success state shows the "You're on the list" / "While you wait" variant. Cal.com, Calendly and Google appointment pages get the README's parameter rules (`components/forms/demo-form.tsx`, `schedulerFor`). | Set `NEXT_PUBLIC_SCHEDULER_URL=https://cal.com/<user>/<event>`. |
| 4 | Sign-in URL (`#signin` stub in every header) | **Open — stub.** | Set `NEXT_PUBLIC_SIGN_IN_URL` (used by `components/site/site-header.tsx` and the mobile drawer). |
| 5 | Cookie-Preferences bracketed placeholders | **Open — content.** `[SERVER LOG CLAIM …]` (Privacy §10 and Cookies §4) and `[SESSION COOKIE NAME]` `[SESSION LIFETIME]` `[CSRF COOKIE NAME]` (Cookies §3) are rendered exactly as in the design (dashed rust chips) so they are impossible to miss. | Edit `app/legal/cookies/page.tsx` and `app/legal/privacy/page.tsx` once the deployment facts are known; if App Bridge means no first-party cookies, rewrite Cookies §3 per DEV-HANDOFF. Note DEV-HANDOFF's open point: loading `google.com/recaptcha/api.js` on `/contact` and `/demo` sets Google cookies, which Cookies §4 ("the marketing site sets no cookies") does not yet reconcile. |
| 6 | Sub-processors described by role, not vendor (Privacy / Security / GDPR) | **Open — legal.** Copy ported verbatim ("Search API provider", "AI model provider"). Nothing added or changed. | Counsel to confirm role-only disclosure satisfies GDPR Art. 13/28 and Shopify's app requirements, or publish a named sub-processor list. |
| 7 | Responsive breakpoints (not designed) | **Done, unreviewed by design.** 360–1920 implemented per the rules given on 2026-09-06; every decision is logged in `MOBILE-NOTES.md`; screenshots at 360/390/768/1024/1280/1440 for all 26 routes in `qa/screens/`. | Designer review of `MOBILE-NOTES.md` + `qa/screens/`. |

Also from DEV-HANDOFF (not in the README's numbered list):

- `#start-free-trial` — Pricing plan buttons, Pricing CTA, homepage CTA. Needs the Shopify App Store listing URL (`app/pricing/page.tsx` `START_TRIAL_HREF`, `app/page.tsx`).
- Facts asserted in copy (read-only scopes, PII stripping test, per-store DB scoping, Cloud KMS on us-east1, 30-day raw-response retention, GDPR webhooks, plan tiers / SKU definition, entity details, no certifications) were ported verbatim and are **not verified against the application**.
- Pricing FAQ "trial end" answer is drafted, unconfirmed (DEV-HANDOFF "Billing rules NOT verified").
